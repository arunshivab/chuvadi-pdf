Add PathGeometry query accessors for vector extraction

Adds Flatten, Bounds, SignedArea, and Contains extension methods on
PathGeometry: adaptive de Casteljau curve flattening, tight axis-aligned
bounds, signed polygon area, and crossing/winding point-in-path for both
fill rules. Pure functions over the segment list with no renderer or
graphics-state dependency. First of the vector-extraction accessor set.